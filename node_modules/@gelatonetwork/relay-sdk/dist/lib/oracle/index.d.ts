import { BigNumber } from "ethers";
import { Config } from "../types";
export declare const isOracleActive: (payload: {
    chainId: number;
}, config: Config) => Promise<boolean>;
export declare const getGelatoOracles: (config: Config) => Promise<string[]>;
export declare const getPaymentTokens: (payload: {
    chainId: number;
}, config: Config) => Promise<string[]>;
export declare const getEstimatedFee: (payload: {
    chainId: number;
    paymentToken: string;
    gasLimit: BigNumber;
    isHighPriority: boolean;
    gasLimitL1: BigNumber;
}, config: Config) => Promise<BigNumber>;

import { Config } from "../types";
export declare const isNetworkSupported: (payload: {
    chainId: number;
}, config: Config) => Promise<boolean>;
export declare const getSupportedNetworks: (config: Config) => Promise<string[]>;

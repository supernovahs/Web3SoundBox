import { Config } from "../types";
import { TransactionStatusResponse } from "./types";
export declare const getTaskStatus: (payload: {
    taskId: string;
}, config: Config) => Promise<TransactionStatusResponse | undefined>;
